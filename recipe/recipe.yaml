schema_version: 1

context:
  name: entwine
  version: "3.2.0"
  sha256: 56abd05efd604d860e43519c03de5c02cd0e99b7cade0401bf0f80f7ba3b67bd

package:
  name: ${{ name|lower }}
  version: ${{ version }}

source:
  url: https://github.com/connormanning/entwine/archive/${{ version }}.zip
  sha256: ${{ sha256 }}

build:
  number: 0
  skip: ppc64le

requirements:
  build:
    - ${{ stdlib("c") }}
    - ${{ compiler('cxx') }}
    - cmake
    - make
    - ninja
  host:
    - libpdal-core
    - curl
    - openssl
  run:
    - libpdal-core
    - curl
    - openssl

tests:
  - script:
      - entwine --version

about:
  license: LGPL-2.1-or-later
  license_file: LICENSE.txt
  summary: Point cloud spatial indexing library
  description: |
    Entwine is a C/C++ library for organizing point clouds as a
    octree tile structure.
  homepage: https://entwine.io
  repository: https://github.com/connormanning/entwine
  documentation: https://entwine.io

extra:
  recipe-maintainers:
    - hobu
    - connormanning
    - chambbj
